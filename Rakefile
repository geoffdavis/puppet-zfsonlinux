begin
  require 'rubygems'
  require 'puppetlabs_spec_helper/rake_tasks'
  require 'git'
  require 'ci/reporter/rake/rspec'
rescue LoadError
  print "Please install all dependencies with the `bundle` command first\n"
  exit
end
